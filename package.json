{
  "name": "furigana",
  "private": true,
  "description": "ふりがなメーカー（yorozu-craft）。依存は辞書（lindera-wasm + IPADIC）だけ。公開物は dict/ に写してコミットする",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "copy-dict": "node tools/copy-dict.mjs",
    "measure": "node tools/measure-tool.mjs"
  },
  "devDependencies": {
    "lindera-wasm-nodejs-ipadic": "2.0.0",
    "lindera-wasm-web-ipadic": "2.0.0"
  }
}
